<%= form_for(barroom) do |f| %>
  <% if barroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(barroom.errors.count, "error") %> prohibited this barroom from being saved:</h2>

      <ul>
      <% barroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>